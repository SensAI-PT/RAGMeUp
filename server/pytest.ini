[pytest]
testpaths = tests
pythonpath = .
markers =
    integration: tests that need ParadeDB or other external services
    unit: fast isolated unit tests
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
addopts = -ra
